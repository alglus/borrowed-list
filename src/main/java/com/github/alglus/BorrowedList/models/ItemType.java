package com.github.alglus.BorrowedList.models;

public enum ItemType {
    // The enums are written in lowercase to make working with them easier across the app.
    // This avoids having additional code, or the URL parameters being CAPITALIZED.
    borrowed, lent;
}
